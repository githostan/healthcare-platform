
apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: appointment-dev
spec:
  selector:
    app: appointment-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      # nodePort: 3080
